import { TrackVisibleDirective } from './track-visible.directive';

describe('TrackVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
